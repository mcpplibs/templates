[package]
name        = "{{project.name}}"
version     = "0.1.0"
description = "A modular C++23 app built on mcpplibs.{{self.name}}"
license     = "Apache-2.0"

[dependencies.mcpplibs]
{{self.name}} = "{{self.version}}"
